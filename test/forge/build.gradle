plugins {
    id("wheel-forge").version("latest.integration")
}

group = "test"
version = "0.1.2"

if (!plugins.hasPlugin("wheel-forge")) {
    return
}

wheel {
    publish = false
}

dependencies {
    include("reflect")
}
