plugins {
    id("wheel")
}

allprojects {
    group = "user11681"
    version = "1.2.3"
}

subprojects {
    afterEvaluate {
        tasks.publish.enabled = false
    }
}

tasks.publish.dependsOn(gradle.includedBuild("plugin").gradle.rootProject.tasks.publish)

if (!plugins.hasPlugin("wheel")) {
    return
}

wheel {
    nospam = false
    publish = false
    channel = "release"
}

repositories {
    mavenLocal()
}

dependencies {
    mod("arrp")
    modInclude("api-screen-handler")
    modInclude("api-screen")
    bloated("auto-config")
    intransitive("mod-menu")
    mod("better-slabs")
}
