plugins {
    id("wheel-forge")
}

group = "test"
version = "0.1.2"

if (!plugins.hasPlugin("wheel-forge")) {
    return
}

wheel {
    publish {
        external {
            repository = null
        }
    }
}

dependencies {
    include("reflect")
}
